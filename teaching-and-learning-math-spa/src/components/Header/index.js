import React from 'react';
import PropTypes from 'prop-types';

MainHeader.propTypes = {
    
};

function MainHeader(props) {
    return (
        <div style={{backgroundColor:"#0099FF", padding: 10}}>
            This is Header
        </div>
    );
}

export default MainHeader;